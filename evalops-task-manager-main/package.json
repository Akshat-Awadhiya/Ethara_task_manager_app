{
  "name": "team-task-manager",
  "private": true,
  "version": "1.0.0",
  "description": "Production-style team task manager with RBAC, task evaluation scores, audit logs, and Railway deployment.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev -w backend\" \"npm:dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "railway:start": "npm run prisma:deploy -w backend && npm run start -w backend",
    "postinstall": "npm run prisma:generate -w backend",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:seed": "npm run seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
